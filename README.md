# Golden Pipit Solutions

The company site: one static page plus its screenshots. No build step — GitHub
Pages serves the repository root as-is.

Every screenshot is demonstration data. Names, figures and companies in them
are invented; nothing here is a real client's record.

To point a custom domain at it, put the hostname in a `CNAME` file at the root
and add the matching DNS record at the registrar.
